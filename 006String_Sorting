#include <iostream>
#include<string>
#include<algorithm>
using namespace std;

// Sorting in a string in Dictionary wise
/*

// 1) Firstly you have to sort the string acoording to the lexographically order
// 2) Secondly you have to sort the string accoding to the length and if length
is same then sort the string the accoding to the lexographically order

inputs 
5
mango shake
apple
grapes
guava
banana

 */



 bool compare(string a,string b)
 {
     if(a.length()==b.length())
     {
     return a<b;
     }
    return a.length()>b.length() ;
    
 }

int main() {

    int n;
    cin>>n;
    string s[100];
    cin.get();

    for(int i=0;i<n;i++)
    {
        getline(cin,s[i]);
    }
    sort(s,s+n,compare);

    for(int i=0;i<n;i++)
    {
        cout<<s[i]<<endl;
    }
    cout<<endl;

}
